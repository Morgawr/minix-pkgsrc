$NetBSD$

--- tools/dbus-launch-x11.c.orig	Wed May  6 17:26:48 2009
+++ tools/dbus-launch-x11.c
@@ -143,7 +143,7 @@ get_session_file (void)
    */
   for (p = display; *p; ++p)
     {
-      if (*p == ':')
+      if (*p == ':' || *p == '/')
         *p = '_';
     }
   
